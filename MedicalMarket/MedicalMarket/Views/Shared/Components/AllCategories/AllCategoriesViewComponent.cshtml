@model List<MedicalMarket.Models.App.Category>

<ul>
    <h3>Browse All Categories</h3>

    @foreach (var item in Model)
    {
        <li><a asp-controller="Home" asp-action="Category" asp-route-id="@item.Id">@item.Name</a></li>
    }
</ul>
